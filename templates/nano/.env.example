# No external services required for the nano template.
# Add any API keys here if your tool calls external services.

# Example:
# VITE_MY_API_KEY=your-key-here

# Note: All VITE_ prefixed variables are exposed to the browser.
# Never put secrets in VITE_ variables in a deployed app.
